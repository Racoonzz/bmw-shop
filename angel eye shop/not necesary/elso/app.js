// Belső állapot let state = {   circle: 0,   square: 0,   rectangle: 0, };   // 1. Selecteld ki id alapján a form elementet és submit eseményre  // köss be funkcionalitást  const shapes = document.getElementById("shapes") shapes.onsubmit = (event) => {   event.preventDefault();   // 2. Az esemény bekövetkezésekor olvasd ki az input mezőkben lévő adatokat  //const selectedShape = document.getElementsByName("selectedShape") [0]; //const action = document.getElementsByName("action"); //console.log(selectedShape.value); //console.log  let action = event.target.elements.action.value;  let shapeName = event.target.elements.selectedShape.value;  // 3. Az adatok alapján módosítsd az alkalmazás belső állapotát    state[shapeName] = action === "increment" ? state[shapeName] +1 : state[shapeName] - 1; render(); }; // 4. A belső állapot alapján rajzold újra a formákat function render() {   document.getElementById("sh-circle").innerHTML = state.circle;   document.getElementById("sh-square").innerHTML = state.square;   document.getElementById("sh-rectangle").innerHTML = state.rectangle; }